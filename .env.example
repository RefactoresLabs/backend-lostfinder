# Configurações do banco de dados
SGBD=sqlite
DATABASE=lostfinder.db

# Chave secreta para assinar os tokens JWT
# Gere uma chave forte: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=sua_chave_secreta_aqui
